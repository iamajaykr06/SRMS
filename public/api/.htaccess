# API Directory .htaccess
# Enable URL rewriting for API endpoints

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Route all API requests to results.php
    RewriteRule ^(.*)$ results.php [L,QSA]
</IfModule>
